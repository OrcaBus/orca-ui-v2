export const DEFAULT_PAGE_SIZE = 20;

// data table: for data table pagination implementation (e.g. DataTable, ExpandableTable)
export const DEFAULT_PAGE_SIZE_OPTIONS = [10, 20, 50, 100];

// Mainly for component that do not have pagination implementation (e.g. dropdowns)
export const DEFAULT_NON_PAGINATE_PAGE_SIZE = 100;

// list table: items per page options for larger datasets
export const DEFAULT_ITEMS_PER_PAGE_OPTIONS = [3, 10, 20, 50, 100];
