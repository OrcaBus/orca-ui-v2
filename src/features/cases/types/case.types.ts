/**
 * Case-related types. Re-exported from shared mockData for feature ownership.
 * Replace with API/DTO types when integrating real backend.
 */
export type { Case } from '../../../data/mockData';

export type CaseTypeVariant = 'blue' | 'green' | 'purple' | 'amber';
